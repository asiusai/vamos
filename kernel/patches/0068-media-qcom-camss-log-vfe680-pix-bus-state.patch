diff --git a/drivers/media/platform/qcom/camss/camss-vfe-17x.c b/drivers/media/platform/qcom/camss/camss-vfe-17x.c
--- a/drivers/media/platform/qcom/camss/camss-vfe-17x.c
+++ b/drivers/media/platform/qcom/camss/camss-vfe-17x.c
@@ -255,7 +255,24 @@
 #define VFE_BUS_WM_FRAME_INC(n)			(0x2258 + (n) * 0x100)
 #define VFE_BUS_WM_BURST_LIMIT(n)		(0x225c + (n) * 0x100)
 
+/* QCS6490 downstream VFE680 BUS v3 register window. */
+#define VFE680_TOP_IRQ_MASK(n)			(0x034 + (n) * 4)
+#define VFE680_TOP_IRQ_STATUS(n)		(0x044 + (n) * 4)
+#define VFE680_TOP_DIAG_SENSOR_STATUS(n)	(0x054 + (n) * 4)
+#define VFE680_TOP_BUS_VIOLATION_STATUS		(0x0c64)
+#define VFE680_TOP_BUS_OVERFLOW_STATUS		(0x0c68)
+#define VFE680_BUS_IRQ_MASK(n)			(0x0c18 + (n) * 4)
+#define VFE680_BUS_IRQ_STATUS(n)		(0x0c28 + (n) * 4)
+#define VFE680_BUS_CLIENT_CFG(n)		(0x0e00 + (n) * 0x100)
+#define VFE680_BUS_CLIENT_ADDR(n)		(0x0e04 + (n) * 0x100)
+#define VFE680_BUS_CLIENT_FRAME_INC(n)		(0x0e08 + (n) * 0x100)
+#define VFE680_BUS_CLIENT_IMAGE_CFG0(n)		(0x0e0c + (n) * 0x100)
+#define VFE680_BUS_CLIENT_IMAGE_CFG1(n)		(0x0e10 + (n) * 0x100)
+#define VFE680_BUS_CLIENT_IMAGE_CFG2(n)		(0x0e14 + (n) * 0x100)
+#define VFE680_BUS_CLIENT_PACKER_CFG(n)		(0x0e18 + (n) * 0x100)
+#define VFE680_BUS_CLIENT_ADDR_STATUS0(n)	(0x0e70 + (n) * 0x100)
+
 #define VFE_PIX_STALL_SOF_GAP			20
 #define VFE_PIX_RECOVER_SOF_GAP		1
 #define VFE_PIX_PAIR_RECOVER_SOF_GAP	20
 
@@ -342,6 +359,41 @@ static void vfe_pix_maybe_log_stall(struct vfe_device *vfe, u32 irq0, u32 irq1,
 		id, vfe_pix_last_ready_idx[id], vfe_pix_last_next_idx[id],
 		vfe_pix_last_prog_idx[id][0], vfe_pix_last_prog_idx[id][1],
 		vfe_pix_last_active_num[id], vfe_pix_last_seq[id]);
+
+	dev_warn_ratelimited(vfe->camss->dev,
+		"vfe%u pix vfe680 top mask0=0x%08x mask1=0x%08x irq0=0x%08x irq1=0x%08x diag0=0x%08x diag1=0x%08x bus_viol=0x%08x bus_ovf=0x%08x busmask0=0x%08x busmask1=0x%08x busirq0=0x%08x busirq1=0x%08x\n",
+		id,
+		readl_relaxed(vfe->base + VFE680_TOP_IRQ_MASK(0)),
+		readl_relaxed(vfe->base + VFE680_TOP_IRQ_MASK(1)),
+		readl_relaxed(vfe->base + VFE680_TOP_IRQ_STATUS(0)),
+		readl_relaxed(vfe->base + VFE680_TOP_IRQ_STATUS(1)),
+		readl_relaxed(vfe->base + VFE680_TOP_DIAG_SENSOR_STATUS(0)),
+		readl_relaxed(vfe->base + VFE680_TOP_DIAG_SENSOR_STATUS(1)),
+		readl_relaxed(vfe->base + VFE680_TOP_BUS_VIOLATION_STATUS),
+		readl_relaxed(vfe->base + VFE680_TOP_BUS_OVERFLOW_STATUS),
+		readl_relaxed(vfe->base + VFE680_BUS_IRQ_MASK(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_IRQ_MASK(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_IRQ_STATUS(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_IRQ_STATUS(1)));
+	dev_warn_ratelimited(vfe->camss->dev,
+		"vfe%u pix vfe680 full0 cfg=0x%08x addr=0x%08x inc=0x%08x img0=0x%08x img1=0x%08x img2=0x%08x pack=0x%08x st0=0x%08x full1 cfg=0x%08x addr=0x%08x inc=0x%08x img0=0x%08x img1=0x%08x img2=0x%08x pack=0x%08x st0=0x%08x\n",
+		id,
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_CFG(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_ADDR(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_FRAME_INC(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_IMAGE_CFG0(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_IMAGE_CFG1(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_IMAGE_CFG2(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_PACKER_CFG(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_ADDR_STATUS0(0)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_CFG(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_ADDR(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_FRAME_INC(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_IMAGE_CFG0(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_IMAGE_CFG1(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_IMAGE_CFG2(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_PACKER_CFG(1)),
+		readl_relaxed(vfe->base + VFE680_BUS_CLIENT_ADDR_STATUS0(1)));
 }
 
 static void vfe_pix_recover_stalled_partial(struct vfe_device *vfe)
