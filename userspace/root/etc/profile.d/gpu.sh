export DEV="${DEV:-CL}"
export GMMU="${GMMU:-0}"
export NOLOCALS="${NOLOCALS:-1}"
export RUSTICL_ENABLE="${RUSTICL_ENABLE:-freedreno}"
export CL_QCOM_PRIORITY_HINT="${CL_QCOM_PRIORITY_HINT:-high}"
export CL_BRANCH_SKIP_OUTPUT_HEADS="${CL_BRANCH_SKIP_OUTPUT_HEADS:-1}"
export DISABLE_FAST_IDIV="${DISABLE_FAST_IDIV:-1}"
export EMULATED_DTYPES="${EMULATED_DTYPES:-long}"
