export DEV="${DEV:-CL}"
export GMMU="${GMMU:-0}"
export NOLOCALS="${NOLOCALS:-1}"
export RUSTICL_ENABLE="${RUSTICL_ENABLE:-freedreno}"
export CL_QCOM_PRIORITY_HINT="${CL_QCOM_PRIORITY_HINT:-high}"
export CL_BRANCH_SKIP_OUTPUT_HEADS="${CL_BRANCH_SKIP_OUTPUT_HEADS:-1}"
export VISIONBUF_USE_DMA_HEAP="${VISIONBUF_USE_DMA_HEAP:-1}"
export MODEL_CL_FRAME_VIPC_DMABUF="${MODEL_CL_FRAME_VIPC_DMABUF:-1}"
export MODEL_CUSTOM_WARP="${MODEL_CUSTOM_WARP:-1}"
export MODEL_PRELOAD_FRAMES="${MODEL_PRELOAD_FRAMES:-1}"
export MODEL_PREWARP_FRAMES="${MODEL_PREWARP_FRAMES:-1}"
export MODEL_FLUSH_PRELOAD="${MODEL_FLUSH_PRELOAD:-1}"
export POLICY_FUSE_LN9="${POLICY_FUSE_LN9:-1}"
export POLICY_FUSE_LN64="${POLICY_FUSE_LN64:-1}"
export POLICY_FUSE_ADJACENT_PAIRS="${POLICY_FUSE_ADJACENT_PAIRS:-1}"
export POLICY_FUSE_QKV_INPUT_LOCAL="${POLICY_FUSE_QKV_INPUT_LOCAL:-1}"
export MODEL_REQD_WORKGROUP_ATTR_KERNELS="${MODEL_REQD_WORKGROUP_ATTR_KERNELS:-r_16_64_32_4_4_24_3_3}"
export DISABLE_FAST_IDIV="${DISABLE_FAST_IDIV:-1}"
export EMULATED_DTYPES="${EMULATED_DTYPES:-long}"
