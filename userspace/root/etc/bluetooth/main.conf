[General]

# Allow an OS that forgot its local keys to replace an obsolete device bond.
# BlueZ asks the registered agent for confirmation, and bled only confirms
# while the physical pairing window is active.
JustWorksRepairing = confirm
